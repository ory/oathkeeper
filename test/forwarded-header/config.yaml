serve:
  api:
    port: 6061
  proxy:
    port: 6060
    trust_forwarded_headers: true

access_rules:
  repositories:
    - file://./rules.1.json

authenticators:
  noop:
    enabled: true
  anonymous:
    enabled: true

authorizers:
  allow:
    enabled: true
  deny:
    enabled: true

mutators:
  noop:
    enabled: true
