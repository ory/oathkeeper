access_rules:
  repositories: file://../test/update/rules_glob.yaml
  matching_strategy: UNKNOWN
authenticators:
  anonymous:
    enabled: true
authorizers:
  allow:
    enabled: true
mutators:
  noop:
    enabled: true
